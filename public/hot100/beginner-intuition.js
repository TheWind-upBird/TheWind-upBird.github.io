window.HOT100_BEGINNER_INTUITION={
"group-anagrams":{title:"先别管“异位词”，先看这几个单词",example:'["eat", "tea", "tan", "ate"]',observe:'eat、tea、ate 虽然字母顺序不同，但它们都只用了 a、e、t 各一次，所以应该放到同一组。tan 用的是 a、n、t，应该去另一组。',question:'电脑怎样快速判断两个字符串“用了完全相同的字母和次数”？',answer:'一个很直观的办法是把字母排好序：eat → aet，tea → aet，ate → aet。排序结果相同，就放进同一组。后面代码里的 key，就是这个“分组标记”。'},
"longest-consecutive-sequence":{title:"先找一段连续的数字",example:'[100, 4, 200, 1, 3, 2]',observe:'把顺序暂时忽略，你能看到 1、2、3、4 连在一起，所以答案至少是 4。100 和 200 都只能单独成段。',question:'如果数组很乱，怎么快速知道 x+1、x+2……还在不在数组里？',answer:'先把所有数字放进 set，查询某个数字存不存在会很快。并且只有“前一个数不存在”的数字才从它开始往右数，例如 1 前面没有 0，所以从 1 开始数 1→2→3→4。'},
"move-zeroes":{title:"先手动把 0 挪到后面",example:'[0, 1, 0, 3, 12]  →  [1, 3, 12, 0, 0]',observe:'我们真正想保留的是非零数字 1、3、12 的原顺序，只把它们依次往前放。',question:'扫描数组时，怎么知道下一个非零数字应该放到哪里？',answer:'用 slow 记住“下一个非零数应该放的位置”，再让另一个指针 fast 从左往右扫描。看到非零数，就把它放到 slow 位置，然后 slow 往右一步。'},
"container-with-most-water":{title:"先把两根柱子当成水桶两边",example:'height = [1, 8, 6, 2, 5, 4, 8, 3, 7]',observe:'选两根柱子后，能装多少水取决于两件事：两根柱子的距离，以及其中更矮的那一根。',question:'如果左右两根柱子高度不同，下一步该移动哪一边，才有机会得到更大的面积？',answer:'移动较矮的一边。因为宽度一定会变小，如果还保留原来的短板，面积没有机会因为高度变大；换掉短板才可能找到更高的边界。'},
"3sum":{title:"先把三数之和拆成“一个数 + 两数之和”",example:'[-1, 0, 1, 2, -1, -4]，找三个数相加等于 0',observe:'如果先固定一个数，比如 -1，那么剩下的问题就变成：在后面的数里找两个数，相加等于 1。',question:'怎样让“找两个数”更有方向，而不是每两个都试一次？',answer:'先排序。排序后可以用左右指针：和太小就让 left 右移，和太大就让 right 左移。每固定一个数，就把剩下部分当成一个两数问题。'},
"trapping-rain-water":{title:"先看一格水为什么能留下来",example:'height = [4, 2, 0, 3, 2, 5]',observe:'例如高度 2 的位置，左边最高墙是 4，右边还有更高的 5，所以水面最多到 4，这一格能存 4-2=2 的水。',question:'扫描时，怎样知道当前位置的水量已经可以确定？',answer:'维护左右两边目前见过的最高墙。较矮的一侧如果另一边已经有更高边界兜底，那么这一侧当前位置能装多少水就已经确定，可以先处理。'},
"longest-substring-without-repeating-characters":{title:"先找一段“没有重复字符”的连续片段",example:'s = "abcabcbb"',observe:'从开头看，"abc" 没有重复，长度是 3；再加入下一个 a 就重复了，所以左边界要往右移动。',question:'怎样一边向右扫描，一边始终保证当前片段里没有重复字符？',answer:'维护一个窗口 [left, right]。right 不断向右加入字符；如果新字符在当前窗口里出现过，就把 left 移到旧位置的下一格。'},
"find-all-anagrams-in-a-string":{title:"先看什么样的连续片段算“同一组字母”",example:'s = "cbaebabacd"，p = "abc"',observe:'p 有 3 个字母，所以候选片段也一定长度为 3。开头 "cba" 虽然顺序不同，但也正好有 a、b、c 各一次，所以起点 0 是答案。',question:'怎样快速检查每个长度为 3 的窗口，里面的字母数量是否和 p 一样？',answer:'用固定长度滑动窗口，并记录窗口内每个字符出现次数。窗口向右移动一格时，加进一个字符、移出一个字符，再比较计数。'},
"subarray-sum-equals-k":{title:"先看“连续一段的和”怎么从两个总和得到",example:'nums = [1, 1, 1]，k = 2',observe:'前两个数的和是 2，所以有一个答案；后两个数的和也是 2，所以还有一个答案。',question:'如果数组很长，怎样避免每个起点都重新往后累加？',answer:'记录“从开头累加到当前位置”的前缀和。当前前缀和是 prefix，如果以前出现过 prefix-k，那么两次前缀和之间那一段的和正好就是 k。'},
"sliding-window-maximum":{title:"先手动看一个长度为 3 的窗口",example:'[1, 3, -1, -3, 5, 3, 6, 7]，窗口长度 k = 3',observe:'第一个窗口 [1,3,-1] 最大值是 3；向右一格变成 [3,-1,-3]，最大值还是 3。我们每次只需要当前窗口最大的那个候选。',question:'窗口不断移动时，怎样避免每次都把窗口里的 k 个数重新找一遍最大值？',answer:'维护一个双端队列，只保留“未来还有机会成为最大值”的下标。更小而且更早的元素会被淘汰，队首始终对应当前窗口最大值。'},
"minimum-window-substring":{title:"先理解“覆盖”是什么意思",example:'s = "ADOBECODEBANC"，t = "ABC"',observe:'我们要在 s 里找一段连续字符串，里面至少包含 A、B、C 各一次。"ADOBEC" 可以，但还比较长；后面的 "BANC" 更短。',question:'怎样找到一个满足要求的窗口后，再尽量把它缩短？',answer:'右指针先扩张，直到窗口包含 t 需要的所有字符；一旦满足，就不断移动左指针缩小。缩到再少一个字符就不满足时，再继续向右扩张。'},
"maximum-subarray":{title:"先判断：前面的和还值得带上吗？",example:'[-2, 1, -3, 4, -1, 2, 1, -5, 4]',observe:'走到数字 1 时，带着前面的 -2 会得到 -1，还不如直接从 1 重新开始。走到 4 时也是类似：前面的累计已经拖后腿。',question:'每到一个新数字 x，怎么决定“接着前面的连续段”还是“从 x 重新开始”？',answer:'比较 x 和 cur+x。cur 表示“必须在前一个位置结束的最好连续和”。如果 cur+x 还不如 x，就把之前那段丢掉，从 x 重新开始。'},
"merge-intervals":{title:"先看两个区间什么时候该合在一起",example:'[[1,3], [2,6], [8,10], [15,18]]',observe:'[1,3] 和 [2,6] 有重叠，所以可以合成 [1,6]；[8,10] 已经从 8 开始，和 [1,6] 分开，因此不能合。',question:'怎样让所有可能重叠的区间靠在一起，方便从左到右处理？',answer:'先按区间起点排序。之后每个新区间只需要和当前结果中的最后一个区间比较：有重叠就扩展右端点，没有就新开一个区间。'},
"rotate-array":{title:"先把“右移 3 格”画出来",example:'[1,2,3,4,5,6,7] 右移 3 格 → [5,6,7,1,2,3,4]',observe:'最后 3 个数 [5,6,7] 整体跑到了最前面，而原来前面的 [1,2,3,4] 跟在后面，两段内部顺序都没变。',question:'如果要求原地完成，怎么把这两段换位置又保持各自顺序？',answer:'可以用三次反转：整体反转，再分别反转前 k 个和剩余部分。这样两段位置交换了，内部顺序也恢复。'},
"product-of-array-except-self":{title:"先看一个位置的答案到底由谁组成",example:'nums = [1,2,3,4]，位置 1 的答案是 1×3×4 = 12',observe:'对于每个位置 i，我们只需要“左边所有数的乘积”再乘“右边所有数的乘积”，完全不需要把自己乘进去。',question:'怎样不使用除法，也不用为每个位置重新乘一遍？',answer:'第一遍从左往右累计 prefix，把每个位置左边的乘积记下来；第二遍从右往左累计 suffix，再乘进答案。'},
"first-missing-positive":{title:"先想：如果 1、2、3 都在，它们最自然该放哪里？",example:'nums = [3,4,-1,1]',observe:'长度是 4。我们最关心 1、2、3、4 是否存在。最自然的摆法是：1 放下标 0，2 放下标 1，3 放下标 2，4 放下标 3。',question:'能不能直接利用数组自己的下标，标记 1..n 哪些数字出现过？',answer:'可以。看到值 x，就尽量把它交换到下标 x-1。最后从左往右看，第一个 nums[i] 不是 i+1 的位置，对应的 i+1 就是缺失的最小正数。'},
"set-matrix-zeroes":{title:"先看一个 0 会影响哪里",example:'[[1,1,1],[1,0,1],[1,1,1]]',observe:'中间这个 0 会让它所在的整行、整列都变成 0，最后得到 [[1,0,1],[0,0,0],[1,0,1]]。',question:'为什么不能一看到 0 就立刻把整行整列改成 0？',answer:'因为你新写进去的 0 会在后续扫描时被误认为原本就存在的 0，继续错误扩散。先记下哪些行、哪些列需要清零，扫描完再统一修改。'},
"spiral-matrix":{title:"先沿着矩阵最外圈走一圈",example:'[[1,2,3],[4,5,6],[7,8,9]]',observe:'顺序是上边 1,2,3 → 右边 6,9 → 下边 8,7 → 左边 4，最后只剩中间的 5。',question:'走完一圈后，怎样继续处理里面更小的一圈？',answer:'维护 top、bottom、left、right 四条边界。每走完一条边，就把对应边界向内缩一格，然后继续下一圈。'},
"rotate-image":{title:"先观察 90° 旋转后位置怎么变",example:'[[1,2,3],[4,5,6],[7,8,9]] → [[7,4,1],[8,5,2],[9,6,3]]',observe:'原来的第一列 1,4,7 旋转后变成了第一行，但方向反过来成了 7,4,1。',question:'有没有比四个一组手动交换更容易记的变换？',answer:'有：先沿主对角线转置，让行列交换；再把每一行反转。两步合起来正好是顺时针旋转 90°。'},
"search-a-2d-matrix-ii":{title:"先站在右上角看看能排除什么",example:'每行从左到右变大，每列从上到下变大；目标 target = 5',observe:'右上角有一个特殊性质：它左边的数更小，它下面的数更大。',question:'如果当前右上角的数比 target 大，应该往哪走？如果比 target 小呢？',answer:'太大就左移，因为这一列下面只会更大；太小就下移，因为这一行左边只会更小。每一步都能排除整行或整列。'},
"intersection-of-two-linked-lists":{title:"先理解“相交”不是数值相等",example:'A: 4→1→8→4→5\nB: 5→6→1→8→4→5',observe:'这里真正的交点是那个共享的节点 8：从它开始，两条链表后面的节点对象完全是同一串。两个节点刚好都叫 1，并不代表相交。',question:'两条链表长度不同，怎么让两个指针最终在共享节点处对齐？',answer:'一个指针走完 A 后去走 B，另一个走完 B 后去走 A。这样两者总路程相同，长度差被抵消，有交点就会在交点相遇。'},
"reverse-linked-list":{title:"先只反转一根箭头",example:'1 → 2 → 3 → None',observe:'如果当前在 1，我们想把 1.next 从 2 改成 None。但一旦直接改，后面的 2→3 就可能找不到了。',question:'改当前节点 next 之前，最先必须保存什么？',answer:'先保存 nxt = cur.next，再把 cur.next 指向前一个节点 prev。然后 prev、cur 一起往前推进。反转链表的核心就是“先保存后路，再改箭头”。'},
"palindrome-linked-list":{title:"先把链表想成从两头比较",example:'1 → 2 → 2 → 1',observe:'数组可以直接比较最左和最右，但单链表只能往后走，没有办法直接从尾巴往前。',question:'怎样在单链表里制造出“从两头往中间比较”的效果？',answer:'先用快慢指针找到中点，再把后半段反转。这样头部和反转后的后半段都能向前走，逐个比较即可。'},
"linked-list-cycle":{title:"先想象两个人在环形跑道上跑",example:'链表尾部又指回中间某个节点，形成一个环',observe:'如果 slow 每次走 1 步，fast 每次走 2 步，只要两个人都进入同一个环，fast 会不断缩小和 slow 的相对距离。',question:'怎样不用额外集合，就判断链表里有没有环？',answer:'用快慢指针。无环时 fast 会先走到 None；有环时 fast 最终一定会在环里追上 slow。'},
"linked-list-cycle-ii":{title:"先分成两个问题：有环吗？环从哪里开始？",example:'3 → 2 → 0 → -4，然后 -4 又指回 2',observe:'快慢指针先能证明“有环”，但第一次相遇通常发生在环里的某个位置，不一定就是入口 2。',question:'已经在环里相遇后，怎样找到真正的入口？',answer:'让一个指针回到头节点，另一个留在相遇点，然后两者都每次走 1 步。它们下一次相遇的位置就是入环点。'},
"merge-two-sorted-lists":{title:"先像合并两摞已经排好序的牌",example:'1→2→4 和 1→3→4',observe:'每次只看两条链表当前最前面的节点，谁更小就先接到结果后面，然后只移动那一条链表。',question:'怎样避免“结果链表第一个节点”需要特殊处理？',answer:'先放一个假的头节点 dummy。真正结果都接在 dummy 后面，这样每一步都统一写 tail.next，最后返回 dummy.next。'},
"add-two-numbers":{title:"先按小学竖式加法来想",example:'342 + 465 = 807；链表用 2→4→3 和 5→6→4 表示',observe:'链表把个位放在最前面，所以可以直接从头开始逐位相加：2+5=7，4+6=10 要写 0 并向下一位进 1。',question:'每一位除了两个数字，还要额外记住什么？',answer:'记住 carry，也就是进位。每轮算 total = x + y + carry，当前位是 total % 10，新进位是 total // 10。'},
"remove-nth-node-from-end-of-list":{title:"先把“倒数第 n 个”变成两个指针之间的距离",example:'1→2→3→4→5，删除倒数第 2 个，也就是 4',observe:'如果 fast 比 slow 永远领先 2 个节点，那么 fast 快到尾部时，slow 就自然落在目标附近。',question:'为了真正删除目标节点，slow 最好停在哪里？',answer:'停在目标节点的前一个节点，这样可以直接执行 slow.next = slow.next.next。再加一个 dummy，就连删除头节点也能统一处理。'},
"swap-nodes-in-pairs":{title:"先只交换第一对节点",example:'1→2→3→4 变成 2→1→4→3',observe:'第一对原来是 prev→1→2→3，交换后应该变成 prev→2→1→3。关键只是重新连接几根 next 指针。',question:'交换完 1 和 2 后，下一轮应该从哪个节点继续？',answer:'交换后 1 成为这一对的尾巴，所以把 prev 移到 1，下一轮再处理它后面的 3、4。dummy 可以让第一对交换也和后面完全一样。'}
};
(function(){
  const base=buildCards;
  function beginnerCard(p,data){return {id:'intuition',step:'建立直觉',render(){return `${header('learn','知识','建立直觉')}<div class="cardBody"><h2>${esc(data.title)}</h2><div class="bigPrompt">${esc(data.example)}</div><div class="callout"><b>先看发生了什么</b><br><span class="muted">${esc(data.observe)}</span></div><p><b>我们真正要解决的问题：</b>${esc(data.question)}</p><div class="callout"><b>从这里再引出方法</b><br><span class="muted">${esc(data.answer)}</span></div></div>${footer()}`},bind(){bindFooter()}}}
  buildCards=function(p){const cards=base(p);const data=window.HOT100_BEGINNER_INTUITION[p.slug];if(data&&p.slug!=='two-sum')cards[0]=beginnerCard(p,data);return cards;};
})();
