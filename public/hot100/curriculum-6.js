window.HOT100_CURRICULUM=(window.HOT100_CURRICULUM||[]).concat([{"number":142,"slug":"linked-list-cycle-ii","title":"环形链表 II","titleEn":"Linked List Cycle II","difficulty":"Medium","topic":"链表","concept":"先用快慢指针在环内相遇。然后一个指针回到头节点，两者都每次走一步，再次相遇的位置就是入环点。","pattern":"slow = fast = head\n# 先找到环内相遇点\n...\np = head\nwhile p is not slow:\n    p = p.next\n    slow = slow.next\nreturn p","notes":[["第一次相遇","确认有环并得到环内位置"],["一个回头、同步一步","利用距离关系定位入环点"]],"quiz":{"q":"第二阶段两个指针为什么都改成一次走一步？","options":["头到入环点距离与相遇点到入环点的等价距离匹配","为了避免再次相撞","题目要求"],"answer":0,"explain":"Floyd 算法的距离关系保证同步一步时会在入环点相遇。"},"starter":"def detectCycle(head):\n    # 返回入环节点；无环返回 None\n","judge":"a=build_cycle([3,2,0,-4],1); entry=node_at(a,1)\nb=build_cycle([1,2],0); entry2=b\nc=build_cycle([1],-1)\nfor head,exp,name in [(a,entry,'pos=1'),(b,entry2,'pos=0'),(c,None,'no cycle')]:\n    try: check(detectCycle(head) is exp,name,detectCycle(head).val if detectCycle(head) else None)\n    except Exception as e: check(False,name,type(e).__name__+': '+str(e))"},{"number":21,"slug":"merge-two-sorted-lists","title":"合并两个有序链表","titleEn":"Merge Two Sorted Lists","difficulty":"Easy","topic":"链表","concept":"用 dummy 作为结果链表的假头节点。每次比较两个链表当前节点，把较小的接到 tail 后面并移动对应指针。","pattern":"dummy = ListNode(0)\ntail = dummy\nwhile list1 and list2:\n    if list1.val <= list2.val:\n        tail.next = list1; list1 = list1.next\n    else:\n        tail.next = list2; list2 = list2.next\n    tail = tail.next","notes":[["dummy","避免单独处理结果链表第一个节点"],["tail","始终指向合并结果最后一个节点"],["剩余链表","一边耗尽后直接接上另一边"]],"quiz":{"q":"dummy 节点的主要作用是什么？","options":["统一处理结果链表的头节点连接","让链表有序","保存长度"],"answer":0,"explain":"有 dummy 后，每次都只需写 tail.next，不需要特判第一次插入。"},"starter":"def mergeTwoLists(list1, list2):\n    dummy = ListNode(0)\n    tail = dummy\n    # 在这里完成\n","judge":"_cases=[([1,2,4],[1,3,4],[1,1,2,3,4,4]),([],[],[]),([],[0],[0])]\nfor a,b,exp in _cases:\n    try: check(list_values(mergeTwoLists(build_list(a),build_list(b)))==exp,f\"a={a}, b={b}\",list_values(mergeTwoLists(build_list(a),build_list(b))))\n    except Exception as e: check(False,f\"a={a}, b={b}\",type(e).__name__+': '+str(e))"},{"number":2,"slug":"add-two-numbers","title":"两数相加","titleEn":"Add Two Numbers","difficulty":"Medium","topic":"链表","concept":"链表低位在前，像手算加法一样逐位相加。每轮计算 total = x + y + carry，把 total % 10 作为当前位，total // 10 作为进位。","pattern":"carry = 0\nwhile l1 or l2 or carry:\n    x = l1.val if l1 else 0\n    y = l2.val if l2 else 0\n    total = x + y + carry\n    carry, digit = divmod(total, 10)","notes":[["carry","上一位产生的进位"],["divmod(total,10)","同时得到进位和当前位"],["while ... or carry","最后可能还剩一个进位节点"]],"quiz":{"q":"为什么循环条件里还要包含 carry？","options":["最高位相加后可能产生新的进位节点","carry 用来统计长度","没有它会死循环"],"answer":0,"explain":"例如 5+5=10，两个链表都结束后仍需创建值为 1 的新节点。"},"starter":"def addTwoNumbers(l1, l2):\n    dummy = ListNode(0)\n    tail = dummy\n    carry = 0\n    # 在这里完成\n","judge":"_cases=[([2,4,3],[5,6,4],[7,0,8]),([0],[0],[0]),([9,9,9,9,9,9,9],[9,9,9,9],[8,9,9,9,0,0,0,1])]\nfor a,b,exp in _cases:\n    try: check(list_values(addTwoNumbers(build_list(a),build_list(b)))==exp,f\"a={a}, b={b}\",list_values(addTwoNumbers(build_list(a),build_list(b))))\n    except Exception as e: check(False,f\"a={a}, b={b}\",type(e).__name__+': '+str(e))"},{"number":19,"slug":"remove-nth-node-from-end-of-list","title":"删除链表的倒数第 N 个结点","titleEn":"Remove Nth Node From End of List","difficulty":"Medium","topic":"链表","concept":"用 dummy 处理删除头节点的情况。先让 fast 领先 slow n 个节点，再同步移动，最终 slow 停在待删除节点的前一个位置。","pattern":"dummy = ListNode(0, head)\nfast = slow = dummy\nfor _ in range(n): fast = fast.next\nwhile fast.next:\n    fast = fast.next\n    slow = slow.next\nslow.next = slow.next.next\nreturn dummy.next","notes":[["fast 领先 n","建立固定距离"],["slow 停在前驱","删除单链表节点需要修改前一个节点的 next"],["dummy","删除原头节点时也能统一处理"]],"quiz":{"q":"为什么 slow 要停在待删除节点的前一个节点？","options":["单链表删除需要执行 slow.next = slow.next.next","为了读取节点值","为了计算 n"],"answer":0,"explain":"没有前驱指针就无法让链表跳过待删除节点。"},"starter":"def removeNthFromEnd(head, n):\n    dummy = ListNode(0, head)\n    # 在这里完成\n","judge":"_cases=[([1,2,3,4,5],2,[1,2,3,5]),([1],1,[]),([1,2],1,[1])]\nfor vals,n,exp in _cases:\n    try: check(list_values(removeNthFromEnd(build_list(vals),n))==exp,f\"list={vals}, n={n}\",list_values(removeNthFromEnd(build_list(vals),n)))\n    except Exception as e: check(False,f\"list={vals}, n={n}\",type(e).__name__+': '+str(e))"},{"number":24,"slug":"swap-nodes-in-pairs","title":"两两交换链表中的节点","titleEn":"Swap Nodes in Pairs","difficulty":"Medium","topic":"链表","concept":"每次处理 prev 后面的两个节点 a、b。把 prev→a→b→next 改成 prev→b→a→next，然后 prev 移到 a。","pattern":"dummy = ListNode(0, head)\nprev = dummy\nwhile prev.next and prev.next.next:\n    a = prev.next\n    b = a.next\n    a.next = b.next\n    b.next = a\n    prev.next = b\n    prev = a","notes":[["dummy","第一对节点交换后头节点会变化"],["a/b","当前要交换的一对"],["prev = a","交换后 a 成为这一对的尾部，下一轮从它后面继续"]],"quiz":{"q":"交换后为什么 prev 要移动到 a？","options":["交换后顺序是 b→a，a 是已处理部分的最后节点","a 的值更小","为了恢复原顺序"],"answer":0,"explain":"下一对节点位于 a 后面，因此 a 正好成为新的 prev。"},"starter":"def swapPairs(head):\n    dummy = ListNode(0, head)\n    prev = dummy\n    # 在这里完成\n","judge":"_cases=[([1,2,3,4],[2,1,4,3]),([],[]),([1],[1]),([1,2,3],[2,1,3])]\nfor vals,exp in _cases:\n    try: check(list_values(swapPairs(build_list(vals)))==exp,f\"list={vals}\",list_values(swapPairs(build_list(vals))))\n    except Exception as e: check(False,f\"list={vals}\",type(e).__name__+': '+str(e))"}]);